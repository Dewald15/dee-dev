import { ShowSpinner, HideSpinner, Reset } from './ui.actions';

describe('Ui Reducer', () => {
    // let action;
    // let reducer;

    describe('Overview', () => {
        xit(`Updates the ui state by showing or hiding the spinner, or resets the ui state to its initial state.`);
    });

    // describe('ShowSpinner', () => {
    //     it('sets the ui state showspinner property to true', () => {
    //         action = new ShowSpinner();
    //         reducer = uiReducer(uiInitialState, action);
    //         expect(reducer.showspinner).toBeTruthy();
    //     });
    // });

    // describe('HideSpinner', () => {
    //     it('sets the ui state showspinner property to false', () => {
    //         action = new HideSpinner();
    //         reducer = uiReducer(uiInitialState, action);
    //         expect(reducer.showspinner).toBeFalsy();
    //     });
    // });

    // describe('Reset', () => {
    //     it('resets the ui state to its initial state', () => {
    //         action = new Reset();
    //         reducer = uiReducer(uiInitialState, action);
    //         expect(reducer).toEqual(uiInitialState);
    //     });
    // });
});
